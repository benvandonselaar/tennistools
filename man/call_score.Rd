% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_score.R
\name{call_score}
\alias{call_score}
\title{Call a Tennis Score}
\usage{
call_score(server_points, receiver_points)
}
\arguments{
\item{server_points}{Integer. Number of points won by the server (0–4+).}

\item{receiver_points}{Integer. Number of points won by the receiver (0–4+).}
}
\value{
A character string with the official tennis score call.
}
\description{
Converts a raw tennis point score (within a game) to the official tennis
spoken call, as used by umpires. Handles standard points, deuce, and
advantage situations.
}
\examples{
call_score(0, 0)   # "ready? play!"
call_score(1, 2)   # "fifteen, thirty"
call_score(3, 3)   # "deuce"
call_score(4, 3)   # "advantage, server"
call_score(3, 4)   # "advantage, receiver"
call_score(4, 2)   # "game, server"
call_score(2, 4)   # "game, receiver"

}
